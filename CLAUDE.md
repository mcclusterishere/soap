# Claude / Codex / Cursor — stop

Satellite of `mcclusterishere/mccluster`.
Worker: `mccluster` on `https://api.mccluster.org`.
There is no Worker named `mccluster-core`.
Read `AGENTS.md` first. Do not grow a second backend.
