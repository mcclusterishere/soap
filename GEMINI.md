# Gemini

Read `AGENTS.md` and `CLAUDE.md` first. Same law.

McCluster (`mcclusterishere/mccluster` + Cloudflare `mccluster` + Supabase `zmnhbrjyhxzhkxmhkexs`) is the backend and control plane for every product and every client backend. Do not invent a parallel stack. Do not race git pushes from CI. Ship public pages to `matthew.mccluster.org`.